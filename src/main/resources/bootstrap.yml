server:
  servlet:
    context-path: /southsayer

jwt:
  secret: tabler

spring:
  application:
    name: southsayer-backend
    environment: ${ENVIRONMENT:qa}

  datasource:
    url: ${DATABASE_ENV_URL:jdbc:oracle:thin:@rfrlmdboaag01:1521:roaag01}
    username: ${DATABASE_ENV_USERNAME:oaa}
    password: ${DATABASE_ENV_PASSWORD:oaa}
    driver:
      class: oracle.jdbc.driver.OracleDriver

  jpa:
    properties:
      hibernate:
        default_schema: ${DATABASE_ENV_SCHEMA:oaa}
---
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring:
  servlet:
    multipart:
      enabled: true
      # Threshold after which files are written to disk.
      file-size-threshold: 2KB
      # Max file size.
      max-file-size: 200MB
      # Max Request Size
      max-request-size: 215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file:
  upload-dir: downloads

# Spring Actuator
management:
  endpoints:
    web:
      exposure:
        include=*:

---
# SOAP
spc:
  storage.service.url: ${WS_STORAGE_URL:http://rfrlmasoaag01.corp.leroymerlin.com:13080/spc-storage-server/services/WsSpcStorageServiceV3?wsdl}

# Upload to Server
sftp:
  username: ${SFTP_USERNAME:cameleon}
  password: ${SFTP_PASSWORD:DEVcam11r!}
  host: ${SFTP_HOST:rfrlmasoamg02}
  port: ${SFTP_PORT:22}
